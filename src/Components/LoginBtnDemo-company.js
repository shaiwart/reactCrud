import {useState} from 'react'; 

export default function AuthenticationBtn({isLoggedIn, onLogin}) {
    const data = (isLoggedIn===false? 'login' : 'logout'); 

    return(
        <div>
            
        </div>
    )
}